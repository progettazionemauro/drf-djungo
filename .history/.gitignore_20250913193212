# === ESISTENTI (li mantengo) ===

env/                    # cartella "env" (virtualenv creato in locale) → mai su GitHub
venv/                   # cartella "venv" (altro virtualenv) → mai su GitHub
__pycache__/            # cache Python compilata (.pyc dentro sottocartelle)
*.pyc                   # file Python compilati singoli → spazzatura
db.sqlite3              # database SQLite principale in locale → mai committare
.static/                # cartella statica nascosta (probabilmente prove) → ignora
staticfiles/            # cartella generata da collectstatic → rigenerabile, non serve in repo
public/                 # cartella Hugo buildata localmente → rigenerabile, non serve in repo
.settings_debug.log     # file log di debug delle settings → locale, non serve in repo
.manage_posts.log       # log di gestione dei post → locale, non serve in repo

# === AGGIUNTE CONSIGLIATE ===

# Env files (non committare mai segreti)
.env                    # file principale con variabili sensibili
.env.*                  # tutti i file .env.dev, .env.prod, ecc. → non su GitHub
!.env.example           # eccezione: l’unico .env committabile è quello “example” senza segreti

# Password/credenziali locali
hetzner_password.txt    # file con password Hetzner (se usi sshpass) → mai su GitHub

# Backup DB locali (mai su Git)
backups/                # cartella con i backup timestampati del DB
*.sqlite3               # qualunque file SQLite residuo → ignoralo

# Build Hugo (sia root public/ sia tema sgb_start/public/)
sgb_start/public/       # output generato da Hugo → si ricrea, non serve nel repo

# Log generici
*.log                   # tutti i file di log → sempre ignorati

# Cache/artefatti vari
.DS_Store               # file di macOS (se lavori da lì o condividi) → inutile
*.cache                 # file cache generici → inutile
